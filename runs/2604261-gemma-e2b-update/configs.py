import pathlib

import torch


PROJECT_NAME = "GIM-SFT"
RUN_NAME = pathlib.Path(__file__).resolve().parent.name

ARTIFACTS_DIR = pathlib.Path("/mnt/data/shichao/artifacts") / RUN_NAME
FINAL_MODEL_DIR = ARTIFACTS_DIR / "sft-gim"
ARTIFACTS_DIR.mkdir(parents=True, exist_ok=True)

RANDOM_SEED = 42

BASE_MODEL_NAME = "unsloth/gemma-4-E2B-it"
MAX_SEQ_LENGTH = 8192
QUANT_BITS = 4

DATASET_NAME = "Sculpt-AI/GIM-SFT"
DATASET_LEN = 50_000

HIGH_SUBSETS_PROPORTION = 23_294
MID_SUBSETS_PROPORTION = (DATASET_LEN - HIGH_SUBSETS_PROPORTION) // 2
LOW_SUBSETS_PROPORTION = DATASET_LEN - HIGH_SUBSETS_PROPORTION - MID_SUBSETS_PROPORTION

TRAIN_SPLIT = 0.98
TRAIN_SIZE = int(DATASET_LEN * TRAIN_SPLIT)

NUM_GPUS = torch.cuda.device_count()
MICRO_BSZ = 2
GRAD_ACCUM = 16
GLOBAL_BSZ = MICRO_BSZ * GRAD_ACCUM * NUM_GPUS

LEARNING_RATE = 2e-4

ESTIMATED_STEPS = (TRAIN_SIZE // GLOBAL_BSZ) * 1
WARMUP_STEPS = max(1, ESTIMATED_STEPS // 20)
SAVE_STEPS = max(1, ESTIMATED_STEPS // 10)
EVAL_STEPS = SAVE_STEPS
NO_EVAL = True

LORA_R = 32
LORA_ALPHA = 32
LR_SCHEDULER_TYPE = "cosine"
WEIGHT_DECAY = 0.01
